import { Button } from '@mui/material'

export const Start = () => {
    return (
        <Button
            variant="outlined"
            color="primary"
            size="large"
            // sx={{ mt: 4 }}
        >Start</Button>
    )
}
